# Route Reconciliation (#48 / #49)

Generated by static extraction + cross-reference against the real, running code — not a manual review of every line. See "Known limitations" before trusting an individual entry.

## Method

- **Backend inventory**: parsed `backend/src/index.js` for every `app.use(path, router)` / `mountRoute(path, service)` call and every `service.setupRoutes(app)` call (29 services), then extracted every `router.METHOD('/path', ...)` inside each resolved file.
- **Frontend inventory**: parsed every `api.METHOD(`...`)` call in `frontend/src/services/api.js`, prefixed with the real axios `baseURL` (`/api/v1`), with `${...}` template interpolations normalized to `:param` the same way as backend `:id`-style params.
- Cross-referenced by normalized `METHOD path`.

## Results

| | Count |
|---|---|
| Unique backend routes found | 1,831 |
| Unique frontend API calls found | 1,387 |
| Backend routes with no frontend caller | 1,043 |
| Frontend calls with no matching backend route | 599 |

Full raw lists: `docs/registry/_orphan_backend_routes.json`, `docs/registry/_orphan_frontend_calls.json` (not committed as prose — machine-readable, for follow-up triage).

## Known limitations (read before acting on the raw lists)

1. **The ~150 `modules/M0XX/*` generated-module routes are not in this inventory at all.** They're loaded via a dynamic directory-walk loop in `index.js`, not a static `app.use(path, var)` call my extractor matches. The earlier concept-document gap analysis already characterized these as "scaffolded CRUD, no dedicated UI" — that finding stands, but this reconciliation doesn't independently re-verify it.
2. Path-param normalization is heuristic (`:id` and `${x}` both become `:param`); a route and a call with the same shape but semantically different param counts could still false-match or false-miss.
3. Every top offender file was spot-checked, not every one of the 1,043/599 entries individually.

## Confirmed real finding (spot-checked, not assumed)

`GET /api/v1/analytics/overview`, `/insights`, `/platform-stats` — called by `frontendAPI.analyticsAPI` and consumed somewhere in the dashboard — resolve to `analyticsService.js`, whose `setupRoutes`-equivalent mount is **already correctly skipped** at boot (`"Skipping route mount for /api/v1/analytics because no router was exported"` — visible in every boot log this session). The reason: `AnalyticsService` never defines an Express router at all. Its `generateReport()` call chain bottoms out in `fetchCropData()`, `fetchFarmerData()`, `fetchRevenueData()`, `fetchExpenseData()`, and ~15 sibling methods that are **100% hardcoded fake numbers** (`return { total: 150, active: 120, ... }`, literally commented `// Mock implementation - would query database in production`).

**This was deliberately left unfixed.** Wiring a real route to this service would mean serving fabricated dashboard data to users — the platform's own no-fabrication discipline (established and enforced throughout this session: see the `Math.random()`-confidence sweep, the escrow-service honesty fixes) says the current behavior — failing to mount rather than lying — is correct. Making this real requires rewriting ~20 fetch methods against actual schema (crops, farmers, production, revenue, expense tables), which is a real, bounded, but separate task, not a five-minute fix.

## Top hand-built files by orphan-route count (backend routes with no frontend caller)

| Count | File |
|---|---|
| 28 | `databaseManagementRoutes.js` |
| 28 | `publicDomainDataExtractionRoutes.js` |
| 27 | `enterpriseControlRoutes.js` |
| 26 | `communityRoutes.js` |
| 25 | `digitalProductPassportService.js` |
| 23 | `logisticsEnhancementRoutes.js` |
| 23 | `serverManagementRoutes.js` |
| 23 | `researchAndDevelopmentRoutes.js` |
| 22 | `nervousSystemRoutes.js` / `cloudManagementRoutes.js` / `moduleSupportInfrastructureRoutes.js` / `startupEnvironmentRoutes.js` / `informationSharingRoutes.js` / `knowledgeRoutes.js` |
| 21 | `foodSafetyService.js` |
| 20 | `governanceModule.js` |
| 19 | `sapModuleArchitectureRoutes.js` / `indigenousKnowledgeService.js` |

**Read of this pattern**: most of these are secondary/admin CRUD operations (update, delete, moderate, detailed-status variants) on domains where the *primary* read/create operations **are** wired into a page — e.g. `communityRoutes.js`'s create/list endpoints have a frontend caller, but its moderate/report/archive endpoints don't. This matches how `CommunityManagementPage.jsx` and similar pages were built: core flow first, admin/secondary operations scaffolded but not yet given UI controls. Confirming this holds for all ~1,000 entries individually is future work.

## Next steps (not completed here — scope is real work, not a quick pass)

1. For each top-offender file: confirm whether its orphan routes are (a) genuinely-needed admin UI that hasn't been built yet, or (b) speculative scaffolding that should be removed.
2. For the 599 frontend-orphan calls: confirm each is either (a) a genuine backend gap to fill, or (b) a frontend call to a renamed/removed path that needs updating. The `analyticsAPI` group above is the one confirmed case of (b)-adjacent (path exists correctly, service behind it doesn't).
3. Extend the backend-route extractor to walk the M0XX dynamic-loader loop so those ~150 modules are included in a future pass.
