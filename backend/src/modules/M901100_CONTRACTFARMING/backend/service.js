/**
 * Contract Farming & Forward Trade (M901100_CONTRACTFARMING)
 *
 * SKELETON - not implemented. Every method below throws NOT_IMPLEMENTED
 * on purpose rather than returning fake success, per this project's
 * honesty discipline (no fabricated success paths). See SPEC.md in this
 * folder for the closed stakeholder loop this module owns and the real,
 * already-existing services it should compose rather than duplicate.
 */

'use strict';

class ContractfarmingService {
  async createContract(...args) {
    throw new Error('M901100_CONTRACTFARMING.createContract: NOT_IMPLEMENTED - see SPEC.md for the real services this should compose');
  }

  async recordMilestone(...args) {
    throw new Error('M901100_CONTRACTFARMING.recordMilestone: NOT_IMPLEMENTED - see SPEC.md for the real services this should compose');
  }

  async releaseEscrowMilestone(...args) {
    throw new Error('M901100_CONTRACTFARMING.releaseEscrowMilestone: NOT_IMPLEMENTED - see SPEC.md for the real services this should compose');
  }

  async flagBreach(...args) {
    throw new Error('M901100_CONTRACTFARMING.flagBreach: NOT_IMPLEMENTED - see SPEC.md for the real services this should compose');
  }

  async matchBuyerToFarmer(...args) {
    throw new Error('M901100_CONTRACTFARMING.matchBuyerToFarmer: NOT_IMPLEMENTED - see SPEC.md for the real services this should compose');
  }
}

module.exports = new ContractfarmingService();
